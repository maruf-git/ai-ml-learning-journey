# Codeforces Python Template

import sys
input = sys.stdin.readline   # fast input


t = int(input()) # multiple test cases
for _ in range(t):
    
    n = int(input()) # single integers
    arr = list(map(int, input().split()))   # array input using map
    a, b = map(int, input().split()) # two integers in one line using map
    
    # arr = [int(x) for x in input().split()] # array input using list comprehesion
    # a, b = [int(x) for x in input().split()] # two integers in one line using list comprehesion
    
    # solution code here
    
    
